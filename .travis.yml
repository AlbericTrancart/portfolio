language: node_js
node_js:
  - "8"

before_script:
    - npm run build

script: echo "no tests"

deploy:
  provider: pages
  local-dir: dist
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  fqdn: alberic.trancart.net
  on:
    branch: master
